package ${package}.${moduleName}.mapper;


import ${package}.${moduleName}.entity.${className};
import ${package}.${moduleName}.model.${className}Model;
import ${package}.${moduleName}.param.${className}Param;
import java.util.List;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;


/**
 * ${comments}数据库访问层
 *
 * @author ${author}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Mapper extends BaseMapper<${className}> {


    /**
     * 新增
     */
    int save(${className}Model vo);


    /**
     * 删除
     */
    int delete(@Param("${pk.attrname}") String ${pk.attrname});


    /**
     * 修改
     */
    int update(${className}Model vo);


    /**
     * 查询
     * tips；单条
     */
    ${className} get(@Param("${pk.attrname}") String ${pk.attrname});


    /**
     * 查询
     * tips；列表
     */
    List<${className}> list(${className}Param param);


}
